class Solution {
    public int[] decompressRLElist(int[] nums) {
        int arraySize = 0;
        for (int i = 0; i < nums.length; i++){
            arraySize += nums[2*i];
        }
        int[] solution = new int[arraySize];

        for (int i = 0;)
    }
}
